<div class="modal-body" style="text-align: center;">
    <form method="post" action="/editCarCommentByAuthor/{{comment._id}}">
                    <textarea cols="5" class="form-control" name="comment" id="commentArea" rows="5"
                              required="required">{{comment.comment}}</textarea><br/>
                    <input type="hidden" value="{{carID}}" name="carID">
        <div class="modal-footer" style="text-align: center; ">
            <a href="/cars/{{carID}}" class="btn btn-default" style="margin-right: 15px">Cancel</a>
            <input type="submit" class="btn btn-success edit-btn-confirm" value="Save Changes"/>
        </div>

    </form>
</div>
